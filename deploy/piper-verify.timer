# Weekly, a few hours after a nightly backup rather than alongside it.
[Unit]
Description=Weekly Piper backup restore test

[Timer]
OnCalendar=Sun *-*-* 06:20:00
Persistent=true

[Install]
WantedBy=timers.target
